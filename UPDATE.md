# 更新记录

> 版本规则：默认只递增补丁号 c（a.b.c）；除非 layla 明确说明，才修改主版本号 a 或次版本号 b。


## 0.2.9 — 2026-09-09
- 新增**行距**设置（标题与日期统一，默认 1.5 倍，可调 0.5–5，随字号自动缩放）

## 0.2.8 — 2026-09-09
- 点击版本号显示更新内容：改为**优先读取本文件夹 UPDATE.md**（网页服务器访问时自动读取最新）；双击直接打开时受浏览器限制，显示内置同步记录
- 会标阴影**模糊默认值 8 → 3**

## 0.2.7 — 2026-09-09
- 默认布局改为：**名称距上 25%、日期距下 15%**（拖动偏移改为相对默认位置；「恢复默认」和双击复位都回到此基准）

## 0.2.6 — 2026-09-09
- 新增**字间距**设置（letter-spacing，标题与日期统一一个值，默认 3pt，随画面比例等比渲染）
- 拖动名称 / 日期微调时，画面顶部实时显示**距上 / 距下边缘的百分比**

## 0.2.5 — 2026-09-09
- 预览支持 **16:9 / 4:3 画面比例**切换（模拟不同屏幕比例下的效果）
- 字号与拖动偏移统一改为**按画面高度（基准 1080）比例渲染**：预览、全屏、任意屏幕比例（含非 16:9 显示器）下视觉占比恒定——修复「放大后比例不一致」
- 「＋ 新增会标」改为**复制当前会标的全部配置**（内容、样式、字号、偏移）

## 0.2.4 — 2026-09-09
- **修复**：会标名称字体无效的 bug——字体回退串被整体引号包裹，浏览器解析成一个不存在的字体名，一直回退黑体；现正确应用所选字体（华文中宋等）
- 「↺ 恢复默认」现在**同时重置会标**（恢复默认双会标 66/30 + 32/24，日期=当天；名单仍保留）
- 阴影默认距离、模糊均改为 **8pt**
- 「design by layla」右侧新增 GitHub 图标，点击新窗口打开仓库页 `github.com/laylaless/meeting-signs`

## 0.2.3 — 2026-09-09
- 日期输入改为**日期选择器**（内部存储 ISO 格式，画面仍显示「2026年9月9日」中文格式；旧数据自动迁移；新会标默认当天）
- **华文中宋自托管**：字体文件放 `font/STZHONGS.ttf`（@font-face 加载），没有安装该字体的电脑也能正常显示——分发时 font 文件夹要和 index.html 一起拷贝

## 0.2.2 — 2026-09-09
- 初始默认提供**两个会标**：第一个 66/30 号（大横幅）、第二个 32/24 号；新增会标沿用 32/24
- 预览画面里会议名称、日期支持**上下拖动**微调垂直位置（左右锁定居中），**双击复位**；全屏画面沿用拖后位置
- 阴影默认模糊改为 3pt；名称字体默认**华文中宋**（下拉新增该选项）

## 0.2.1 — 2026-09-09
- 新增「④ 会标」：会议名称 + 日期全屏显示（全屏时只显示画面，配置项不显示），↑↓ 切换会标，ESC 退出
- 支持多个会标：列表新增 / 删除 / 点选编辑，列表顺序即 ↑↓ 切换顺序（至少保留一个）
- 会议名称用 textarea 输入、pre 格式渲染（手动换行生效），输入实时预览；日期自定义
- 背景色默认政务红 rgb(165,38,21)、预设 rgb(193,0,0)，拾色器 + 16 进制自定义；文字颜色默认黄（预设金黄 / 白），同样可自定义
- 名称阴影按 PPT 阴影参数风格：颜色 / 透明度 / 角度 / 距离 / 模糊；名称默认 32 号、日期默认 24 号（PPT 字号 pt），字号可调
- 会标数据随「导出 / 导入会场 JSON」备份恢复（旧文件无此字段则不动本机），localStorage 持久化，纳入撤销/重做

## 0.1.8 — 2026-09-09
- 修复加桌后视角跟随不居中：改用 requestAnimationFrame 在下一帧布局完全稳定后取画布尺寸再平移
- 修复「适应」包围盒：改为旋转感知的半宽半高 AABB（原实现把桌子全宽当半径、不感知旋转，导致高桌/竖桌场景 zoom 被过度压缩、内容偏小）
- 修复多桌场景「适应」后两侧桌子被裁出视野：zoom 下限 0.2 → 0.05（滚轮缩放/按钮/适应三处同步调整），20 张长条桌实测全部收入画布、边距对称
- 加桌避让网格 8 列 → 4 列：多桌场地形状接近画布宽高比，「适应」后桌子显示尺寸翻倍（实测 20 桌从 80px/桌提升到 164px/桌），不再缩成一条扁带
- 修复高分屏多桌场景「适应」不居中的真正根因：侧栏内容（桌子列表/人员面板）把画布 CSS 高撑到 3000+px、大部分在屏幕外，「适应」按整幅画布居中导致视口内看偏。侧栏改 sticky 自滚动、画布限高不超过视口（max-height: calc(100vh - 230px)）；实测 58 桌 + retina（dpr=2）：画布 670px 完全在视口内，内容中心与画布中心精确重合

## 0.1.7 — 2026-09-09
- 新增会议桌自动避开已放置的桌子：从默认起点按网格搜索第一个不重叠的空位（AABB 近似，含座位余量）
- 添加桌子后画布视角平移到新桌子中心（缩放比例 zoom 保持不变，仅移动 view）
- 「会场编辑 2.0」标题改为「会场编辑」

## 0.1.6 — 2026-09-09
- 撤销/重做按钮移至画布右上角：圆形图标 ↶ ↷（banner 文字版移除）
- 新增重做：Ctrl/Cmd+Shift+Z；新操作会清空重做栈（标准撤销/重做行为）

## 0.1.5 — 2026-09-09
- 新增撤销：banner「↶ 撤销」按钮 + Ctrl/Cmd+Z 快捷键；快照式实现（save 时存前一版状态，最多 50 步），覆盖加桌/拖动/排座/改名/导入/恢复默认等所有改动；输入框聚焦时快捷键不拦截；撤销不跨刷新会话

## 0.1.4 — 2026-09-09
- 版本号与「design by layla」交换位置：版本号在前（v0.1.4｜design by layla）

## 0.1.3 — 2026-09-09
- 版本号移至「design by layla」右侧，用「｜」分隔并排显示
- 长条桌左右端面座位增多时，桌子高度随之自动加长（端面座位竖排不再悬空超出桌体）

## 0.1.2 — 2026-09-09
- 所有桌型单侧座位上限统一调整为 50（长条桌上/下侧、圆桌、主席台；原 99/24）
- 长条桌左右端面座位上限从 2 放开到 50

## 0.1.1 — 2026-09-09
- 台签打印字体新增「华文楷体」（STKaiti）
- 台签打印支持自定义字体：下拉选「自定义字体…」后输入任意字体名（需本机已安装）
- 取消标准模版锁定华文新魏的限制，所有模版均可自由选择字体
- 长条桌上/下侧座位数取消 20 上限，支持任意个（0~99），桌宽随座位数自动加长
- 页面左上角显示版本号（隐蔽小字），点击弹窗查看更新记录

（此前变更未版本化，自 0.1.1 起记录。）

> 维护提醒：每次改版时同步两处——本文件、`index.html` 顶部 `#app-ver` 文本；内置兜底记录用脚本从本文件重新生成（JSON 转义嵌入 `UPDATE_LOG`）。
